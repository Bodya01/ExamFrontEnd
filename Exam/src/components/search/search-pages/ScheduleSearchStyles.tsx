import { StyleSheet } from "react-native";

const ScheduleSearchStyle = StyleSheet.create({
    pageWrapper: {
        flex: 1,
        alignItems: "center"
    },
})

export default ScheduleSearchStyle;