import { StyleSheet } from "react-native";

const HeaderBackButtonStyles = StyleSheet.create({
    buttonWrapper: {
        flexDirection: "row",
        alignItems: "center",
        marginRight: 20
    },
    buttonText: {
        fontSize: 20,
    },
})

export default HeaderBackButtonStyles;